<h3>To-Do List</h3>
<p class="text-muted">Daftar kegiatan otomatis. Setelah semua selesai dan kamu tekan Update, daftar akan diperbarui otomatis.</p>

<form action="/tasks/update" method="post" class="card p-3 shadow-sm">
  <ul class="list-group">
    <% tasks.forEach((task, idx) => { %>
      <li class="list-group-item d-flex align-items-center">
        <input class="form-check-input me-2" type="checkbox" name="done" value="<%= idx %>" <%= task.done ? "checked" : "" %>>
        <div><%= task.text %></div>
      </li>
    <% }) %>
  </ul>

  <div class="mt-3">
    <button class="btn btn-primary" type="submit">Update</button>
  </div>
</form>
